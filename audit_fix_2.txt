# ARCHITECTURAL REVIEW REJECTION
Your 27/27 test pass rate is an illusion because you took unacceptable shortcuts and violated the core directives of the prompt. I am rejecting this current state. 

You must execute the following fixes immediately:

## 1. Fix the S3 Materialization (No Warnings, Actual Code)
Adding a docstring warning is unacceptable. 
* Refactor `s3.get_object()` so it streams the `StreamingBody` response (e.g., using `.iter_chunks()`) rather than loading it entirely into memory.
* Refactor `s3.list_keys()` to use AWS Pagination (`get_paginator('list_objects_v2')`) and `yield` the keys as a Python `Generator`.

## 2. Real Integration Testing (No Mocks)
You completely ignored the LocalStack directive. `unittest.mock.patch` does not test the network boundary or real `botocore` error structures.
* Create a `docker-compose.yml` at the project root defining a `localstack/localstack` service.
* Write a new test file (`tests/test_integration.py`) that points the `boto3` client `endpoint_url` to LocalStack and actually tests S3 bucket operations and SQS queue polling.

## 3. Session Dependency Injection
Your dictionary cache is an improvement, but you failed to add the explicit `session` parameter. Update the public facade functions to accept an optional `session: boto3.Session` object. If the user provides a `session`, use it directly and bypass your internal cache entirely.

Do not stop until these three specific architectural requirements are fully implemented in code.